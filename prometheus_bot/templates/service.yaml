apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus_bot.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "prometheus_bot.name" . }}
    helm.sh/chart: {{ include "prometheus_bot.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "prometheus_bot.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
